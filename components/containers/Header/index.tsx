import { Container } from '@Commons'

import * as Styled from './styles'

const Header = () => {
  return (
    <Styled.Header>
      <Container>
        <Styled.HeaderTitle>Extrato</Styled.HeaderTitle>
      </Container>
    </Styled.Header>
  )
}

export default Header
